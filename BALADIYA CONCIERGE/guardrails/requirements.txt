# ============================================================
# Guardrails sidecar — NO torch, NO transformers
# Pattern-based rail detection + Presidio regex PII detection
# ============================================================

fastapi==0.111.1
uvicorn[standard]==0.30.1

httpx==0.27.0

# PII detection — regex PatternRecognizers (no NLP model for API calls)
presidio-analyzer==2.2.35
presidio-anonymizer==2.2.35
spacy==3.7.4
# en_core_web_sm downloaded in Dockerfile

pydantic==2.8.2
pydantic-settings==2.3.4

structlog==24.2.0
hvac==2.3.0
PyYAML==6.0.1
